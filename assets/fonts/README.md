# Self-hosted fonts

This directory contains the Latin WOFF2 subsets used by the portfolio:

- Inter, weights 300-600
- Playfair Display, weights 600-700

The files replace Google Fonts runtime requests so typography is served from
the same origin as the rest of the static portfolio assets.
